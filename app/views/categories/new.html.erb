<h2><%= t('category.new') %></h2>

<%= error_messages_for :category %>

<% form_for([@container, @category]) do |f| %>
  <%= render :partial => 'form', :object => f %>
  <p>
    <%= f.submit t(:create) %>
  </p>
<% end %>
