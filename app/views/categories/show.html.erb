<h1><%= sanitize @category.name %></h1>
<p>
<%= sanitize @category.description %>
</p>

<% @categorizables.each do |t| %>
  <% div_for(t) do %>
  <%= link_logotype t, :size => 64, :spacer => '<br />' %>
  <% end %>
<% end -%>
