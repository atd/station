<h2><%= title %></h2>

<%= sortable_list(@categories, Category, :path => [current_container, Category.new]) %>

<br />

<%= link_to 'New category'.t, new_polymorphic_path([ @container, Category.new ]) %>
